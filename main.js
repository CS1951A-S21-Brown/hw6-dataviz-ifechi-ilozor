const MAX_WIDTH = Math.max(1080, window.innerWidth);
const MAX_HEIGHT = 720;
const margin = {top: 40, right: 100, bottom: 40, left: 425 };

let graph_1_width = MAX_WIDTH / 1.25, graph_1_height = 250;
let graph_2_width = MAX_WIDTH / 1.65, graph_2_height = 400;
let graph_3_width = 1080, graph_3_height = 575;
